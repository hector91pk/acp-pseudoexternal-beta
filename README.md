# ACP Pseudoexternal Beta Repo

Repositorio pequeño y controlado para validar el flujo ACP en un contexto pseudoexterno.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pytest -q